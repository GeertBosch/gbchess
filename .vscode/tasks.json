{
    "version": "2.0.0",
    "tasks": [
        {
            "type": "cargo",
            "command": "build",
            "group": "test",
            "problemMatcher": [
                "$rustc"
            ],
            "label": "rust: cargo build moves_gen",
            "options": {
                "cwd": "${workspaceFolder}/rust/moves_gen"
            }
        }
    ]
}